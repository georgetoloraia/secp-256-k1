1. from steps.py i need save all result.x and addend.x in 
    all_hex.txt without 0x
    if result.x not in all_hex.txt:
        all_hex.txt.append(result.x)
    if addend.x not in all_hex.txt:
        all_hex.txt.append(addend.x)